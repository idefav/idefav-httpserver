apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idefav-httpserver.fullname" . }}-test-connection"
  labels:
    {{- include "idefav-httpserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idefav-httpserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "idefav-httpserver.fullname" . }}
  labels:
    {{- include "idefav-httpserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "idefav-httpserver.selectorLabels" . | nindent 4 }}
